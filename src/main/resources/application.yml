server:
  port: 8010

spring:
  application:
    name: abacus-work-svc
  security:
    user:
      name: admin
      password: admin

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://admin:admin@localhost:8761/eureka/